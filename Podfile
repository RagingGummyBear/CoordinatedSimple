# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'CoordinatedSimple' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CoordinatedSimple
  pod 'ReachabilitySwift'
  pod 'CryptoSwift'
  pod "PromiseKit", "~> 6.8"

  target 'CoordinatedSimpleTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CoordinatedSimpleUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
